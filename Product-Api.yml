spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/ecomm
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  boot:
    admin:
      client:
        url: http://localhost:2222/
  data:
    rest:
      base-path: /ecomm
server:
  port: 8080